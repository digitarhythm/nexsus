/*******************************************************************
********************************************************************
********************************************************************

 NEXSUS GAME Create System created 2001.07.19
 All headers

********************************************************************
********************************************************************
********************************************************************/

#include "nexsus.h"

#define	PRINTNUM( b )	{sprintf(debug,"%d",b);puts(debug);}
#define	PRINTPTR( b )	{sprintf(debug,"%x",b);puts(debug);}
#define	PRINTSTR( b )	{sprintf(debug,"%s",b);puts(debug);}
#define	PRINTDBL( b )	{sprintf(debug,"%lf",b);puts(debug);}
